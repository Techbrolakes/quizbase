import RootLayout from '@/config/layout/RootLayout';
import React from 'react';

const Homepage: React.FC = () => {
   return (
      <RootLayout>
         <div className="text-5xl font-bold">Homepage</div>
      </RootLayout>
   );
};

export default Homepage;
